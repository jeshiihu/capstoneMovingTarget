/*
 * motorConstants.c
 *
 *  Created on: Mar 10, 2018
 *      Author: jhuynh
 */

#define FPGA_TO_HPS_LW_ADDR(base)  ((void *) (((char *)  (ALT_LWFPGASLVS_ADDR))+ (base)))
#include "motorConstants.h"

void* GetMotorStepBaseAddr(enum Motor motor)
{
	if(motor == motorX)
		return (void*)STEP_BASE_Y;

	return (void*)STEP_ADDR_Y;
}

void* GetMotorDirBaseAddr(enum Motor motor)
{
	if(motor == motorX)
		return (void*)DIR_ADDR_X;

	return (void*)DIR_ADDR_Y;
}

void* GetMotorPeriodBaseAddr(enum Motor motor)
{
	if(motor == motorX)
		return (void*)PERIOD_ADDR_X;

	return (void*)PERIOD_ADDR_Y;
}

void* GetMotorDutyBaseAddr(enum Motor motor)
{
	if(motor == motorX)
		return (void*)DUTY_ADDR_X;

	return (void*)DUTY_ADDR_Y;
}

